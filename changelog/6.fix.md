- Included 2022 in the output files. This reflects a (currently not well-documented) agreement that scenario files should always start in 2022.
- Fixed `extract_fossil_biosphere_timeseries` so it only dropped years where all values were `NaN` (previously it dropped columns where any values were `NaN`)
